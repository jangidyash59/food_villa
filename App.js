//import React from "react"
import ReactDOM from "react-dom/client"
const heading = (
    <h1 key="100">Welcome to Food Villa</h1>
);
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(heading);